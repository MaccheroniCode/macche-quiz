name: Demo quiz
questions:
  - heading: |
      # Wich one is correct for 1?
      ```ts
        import { memo } from 'react';
        import Markdown from 'react-markdown';
        import remarkGfm from 'remark-gfm';

        interface MarkdownContentProps {
          readonly markdown: string;
        }

        const MarkdownContent = memo<MarkdownContentProps>(({ markdown }) => {
          return (
            <Markdown remarkPlugins={[remarkGfm]}>{markdown}</Markdown>
          );
        });

        export default MarkdownContent;
      ```
    multi: true
    answares:
      - answare: |
          Correct 1
          ```ts
            import { memo } from 'react';
            import Markdown from 'react-markdown';
            import remarkGfm from 'remark-gfm';

            interface MarkdownContentProps {
              readonly markdown: string;
            }

            const MarkdownContent = memo<MarkdownContentProps>(({ markdown }) => {
              return (
                <Markdown remarkPlugins={[remarkGfm]}>{markdown}</Markdown>
              );
            });

            export default MarkdownContent;
          ```
        correct: true
      - answare: |
          Wrong 1
          ```js
            bbbb
          ```
      - answare: |
          Correct 2
          ```js
            cccc
          ```
        correct: true
      - answare: |
          Wrong 2
          ```js
            dddd
          ```
  - heading: |
      # Wich one is correct for 2?
    answares:
      - answare: |
          Correct 1
          ```js
            aaaa 2
          ```
        correct: true
      - answare: |
          Wrong 1
          ```js
            bbbb 2
          ```
      - answare: |
          Wrong 2
          ```js
            cccc 2
          ```
      - answare: |
          Wrong 3
          ```js
            dddd 2
          ```
